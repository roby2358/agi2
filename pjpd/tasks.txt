Priority:   92
Status: Done
ID: dense-tgt-r4mk
Created: 2026-09-04T18:05:00Z
Updated: 2026-09-04T18:30:00Z

agi2/semantic: dense training targets — train every position, not just each window's last. _compute_batch_loss (src/training.py) currently takes only the hidden state at the last real prompt position against the single next token, so a ~250-token window yields ONE training signal; a causal LM extracts one per position from the same forward pass. Over a full lilwill_rwkv run that is ~900K token-level signals where standard training would see ~136M (~150x deficit) — the likely reason raw_gap plateaus by epoch 30. Change: for every real position t, the target is the next token (prompt_ids[t+1] inside the window; target_ids[0] at the last real position; padding positions excluded via prompt_mask), flatten all valid (hidden, target-embedding) pairs and feed them to PairwiseCosineLoss unchanged — its forward is shape-agnostic and num_pairs already scales with N. Behind a dense_targets TOML flag (default true; false = the old last-position path, kept as the control). valid_observations in the metrics will jump from ~batch to ~token-budget (~37k) — expected. Tests for the next-token-target construction (interior positions, last real position, padding exclusion) in tests/test_training.py. Keeps the cosine objective untouched — this is purely a signal-density fix.
----
Priority:   91
Status: Done
ID: infonce-w2qh
Created: 2026-09-04T18:05:00Z
Updated: 2026-09-04T19:10:00Z

agi2/semantic: InfoNCE objective — cross-entropy over cosine logits. The current sigmoid-gap loss pulls hidden states toward target embeddings but exerts almost no downward pressure on the 4,095 wrong tokens (only sampled pairs), so cosine scores stay packed and sampling looks random even when the representation knows the answer (measured: <|endoftext|> ranks top-3 at true utterance ends but p≈0.018 under the softmax). Replace (or make selectable alongside) the geometric/anchor loss: logits = cosine(hidden, every vocab embedding) / learnable-or-config temperature, then standard NLL against the true next token — this IS cosine-similarity training with proper contrastive normalization (InfoNCE), and it makes training optimize exactly what generation samples from (generation already scores by cosine-softmax). Keep the geometric term available as an auxiliary (config ratio). Requires dense-tgt-r4mk (per-position targets) to be worthwhile. Expected effects: sharper sampling distribution, coherent word boundaries, EOT stopping firing naturally, less need for repetition penalty. TOML: objective = "infonce" | "pairwise", nce_temperature. Update SPEC_SIMILARITY_TRAINING.md with the rationale and keep the pairwise path as the research control.
----
Priority:   90
Status: ToDo
ID: ce-control-p8vn
Created: 2026-09-04T18:05:00Z
Updated: 2026-09-04T18:05:00Z

agi2/semantic: plain cross-entropy control run — the missing baseline for the cosine-training experiment. Same architecture (RWKVModel and/or AGI2Model), standard logits (model.forward already projects to vocab) + F.cross_entropy on per-position next tokens, identical data/curriculum/epochs as lilwill_rwkv. Deliverables: a ce (or objective = "ce") training path sharing the dense-target machinery from dense-tgt-r4mk, a trained control checkpoint, held-out perplexity for both models, and a side-by-side sample comparison. Reference expectation: nanoGPT-tiny at this scale (~1M-token Shakespeare corpus, ~10M params) produces recognizable pseudo-Shakespeare — if the control reaches that and the cosine models do not, the gap measures what the cosine objective costs; if it does not, the bottleneck is elsewhere (data/architecture). Do not retire the cosine path — the point is to quantify it, not replace it.
----
Priority:   90
Status: Done
ID: eot-token-azmi
Created: 2026-09-03T19:31:16Z
Updated: 2026-09-04T07:20:00Z

agi2/semantic: register special tokens <|endoftext|>, <|break|>, and <|pad|> in BPETokenizer alongside the existing <EOS> (special_tokens list in BpeTrainer, plus the vocab dict built in fit() and load_vocab()). <|endoftext|> already appears literally ~31.6k times in samples/shakespeare_dialogue.txt but currently tokenizes as ordinary subwords (<| + endoftext + |>), so models learn to emit the text yet generation never stops on it — generation only breaks on <EOS>, which never occurs in training data. Once registered it encodes atomically and the model trains on the real token. <|break|> (future intra-document structure) and <|pad|> (future variable-length batching + loss masking) are reserved only — not emitted or consumed anywhere yet. Update src/generation.py to stop sampling on a set of stop-token ids (<EOS> and <|endoftext|>) instead of the single eos id, in both generate_text and generate_with_beam_search, and let the stop tokens through build_corpus_token_mask (never <|pad|>/<|break|>). Decode cleanup, same change: special tokens belong in the token set and in training, but must not appear in returned inference text — strip them at the generation boundary (skip_special_tokens=True on decode of generated output, or strip the trailing marker), so callers never see literal <EOS>/<|endoftext|> in results. Update tests/test_bpe_tokenizer.py and tests/test_generation.py. Note: tokenizer changes take effect only after a tokenizer re-fit + retrain (e.g. resources/lilwill_rwkv.toml); the existing trained/lilwill_rwkv.pt keeps its old 3-subword encoding.
----
Priority:   87
Status: ToDo
ID: primer-39nj

jack jane primer task, 3000 word vocabularly, 1 megabyte
----
Priority:   85
Status: ToDo
ID: script-merge-gfst

Combine the agi2_* scripts into a single script with a command parameter
----
Priority:   80
Status: ToDo
ID: corpus-f8d8

build a synthetic training set in corpus.txt
----
Priority:   75
Status: ToDo
ID: corpus02-az57

Build corpus02.txt: Critical Thinking & Fallacy Detection content
----
Priority:   75
Status: ToDo
ID: corpus03-hv8d

Build corpus03.txt: Problem Decomposition & Pattern Recognition content
----
Priority:   75
Status: ToDo
ID: corpus04-kjk5

Build corpus04.txt: Physics & Physical Sciences content
----
Priority:   75
Status: ToDo
ID: corpus05-a3p9

Build corpus05.txt: Chemistry & Materials Science content
----
Priority:   75
Status: ToDo
ID: corpus06-vbvr

Build corpus06.txt: Biology & Life Sciences content
----
Priority:   75
Status: ToDo
ID: corpus07-4wmx

Build corpus07.txt: Earth & Space Sciences content
----
Priority:   75
Status: ToDo
ID: corpus08-3uvj

Build corpus08.txt: Creative Writing & Storytelling content
----
Priority:   75
Status: ToDo
ID: corpus09-eb3h

Build corpus09.txt: Humor & Wit content
----
Priority:   75
Status: ToDo
ID: corpus10-deqv

Build corpus10.txt: Programming & Computer Science content
----
Priority:   75
Status: ToDo
ID: corpus11-vixv

Build corpus11.txt: Mathematics & Engineering content
----
Priority:   75
Status: ToDo
ID: corpus12-nmxj

Build corpus12.txt: System Design & Architecture content
----
Priority:   75
Status: ToDo
ID: corpus13-qxsv

Build corpus13.txt: Cognitive Psychology & Decision Making content
----
Priority:   75
Status: ToDo
ID: corpus14-ufr8

Build corpus14.txt: Social Psychology & Human Dynamics content
----
Priority:   75
Status: ToDo
ID: corpus15-i2hp

Build corpus15.txt: Rhetoric & Persuasion content
----
Priority:   75
Status: ToDo
ID: corpus16-s64p

Build corpus16.txt: Language & Linguistics content
----
Priority:   75
Status: ToDo
ID: corpus17-mfcd

Build corpus17.txt: Ethics & Moral Philosophy content
----
Priority:   75
Status: ToDo
ID: corpus18-p69u

Build corpus18.txt: Philosophy & Existential Questions content
----
Priority:   75
Status: ToDo
ID: corpus19-twvr

Build corpus19.txt: Reasoning Exercise Drills content
----
Priority:   75
Status: ToDo
ID: corpus20-hsh8

Build corpus20.txt: Rhetorical Melee Drills content
A free-flowing, dynamic exchange of arguments and counter-arguments
----
Priority:   70
Status: Done
ID: wkv-chunked-un6i
Created: 2026-09-02T06:38:54Z
Updated: 2026-09-04T15:40:00Z

Rewrite the RWKV WKV recurrence in semantic/src/rwkv.py (_wkv, currently a Python `for t in range(seq_len)` loop) as a chunked scan: process blocks of timesteps with batched tensor ops instead of one step at a time, carrying the (numerically stable, log-space max-tracking) state between chunks. The current loop launches ~10 tiny CUDA kernels per timestep, so training is kernel-launch-bound — one CPU core pegged, RTX 4090 at ~240W/450W despite 90% reported utilization. Must preserve the stable WKV formulation (float32 internals, max-shift trick), keep mypy strict clean, and produce outputs matching the loop implementation within float tolerance (add a unit test comparing chunked vs. naive on random inputs). Verify GPU-side speedup with a before/after timing of one epoch of lilwill_rwkv.

OUTCOME (2026-09-04, commit 217c7cf): implemented and equivalence-tested (outputs + gradients match the loop, kept as _wkv_loop). GPU verification at epoch 48/seq 244: 353.02s (loop, 11,252 seqs) vs 294.74s (chunked, 9,170 aligned seqs) — 31.4 vs 32.1 ms/seq, i.e. PERFORMANCE-NEUTRAL per sequence; the wall gain was seq-bounds-v5yr's fewer sequences. Finding: cutting WKV launches ~16x did not move per-batch time (~5s, and roughly CONSTANT across seq 151 vs 244), so training was never WKV-launch-bound. The real bottleneck is seq-independent and unidentified — see train-profile task.
----
Priority:   70
Status: Done
ID: train-prof-e7qd
Created: 2026-09-04T15:45:00Z
Updated: 2026-09-05T04:20:00Z

agi2/semantic: find the real training bottleneck. Evidence (2026-09-04, RTX 4090, lilwill_rwkv 13M params): per-batch fwd+bwd time is ~5s and roughly CONSTANT across sequence lengths (epoch 30 seq=151 batch=244: 345.8s/~61 batches; epoch 48 seq=244 batch=151: 294.7s/~61 batches), and was unchanged by cutting WKV kernel launches ~16x (chunked scan, commit 217c7cf) — so the cost is seq-independent and not WKV or the pairwise cosine loss (which samples index pairs, cheap).

ROOT CAUSE FOUND (2026-09-04): nvidia-smi during training showed 24,058/24,564 MiB used at only 108W — the chunked WKV's autograd intermediates (several (B,c,c,D) tensors per chunk) measured 14.58GB saved-for-backward across 6 layers at the training token budget (saved_tensors_hooks measurement), overflowing VRAM into the Windows driver's system-memory fallback; every touch of spilled pages crosses PCIe, giving the flat ~5s/batch. B x seq is constant by the token-budget design, which is why the cost never varied with seq. FIX: per-chunk gradient checkpointing in _wkv (recompute the chunk forward in backward) cuts saved bytes to 0.75GB (19x); equivalence tests still pass through the checkpointed path. Remaining: user restarts training with the fix and reports the new epoch time to confirm the speedup (expected several-x; the old loop's separate CPU-launch bottleneck is also gone). NOTE the old loop was a DIFFERENT ~5s/batch bottleneck (CPU launch overhead at 240W, core pegged) — two causes, coincidentally similar cost.

CONFIRMED (2026-09-05): with per-chunk gradient checkpointing (commit f9efb16), epoch 48 (seq=244) ran in 39.43s vs 294.74s spill-bound and 353.02s on the original loop — 7.5x / 9x at the same epoch; epoch 100 (seq=512) took 52.57s. Full 100-epoch lilwill_rwkv run now completes in under an hour.
----
Priority:   70
Status: ToDo
ID: lora-train-hhgd

Train a LoRA for more consistent conversation

Implementation requirements:
- LoRA layer implementation (rank decomposition matrices)
- Integration with your existing attention and FFN layers  
- LoRA-specific training configuration
- Merge functionality to combine LoRA weights with base model
----
Priority:   65
Status: Done
ID: train-log-m3xa
Created: 2026-09-05T04:25:00Z
Updated: 2026-09-05T06:30:00Z

agi2/semantic: persist training runs. train_model only prints to stdout, so a run's per-epoch history is lost unless the user remembered to tee (the 2026-09-05 infonce milestone run's curve lives only in terminal scrollback). Add: (1) a plain-text log of everything currently printed, written to logs/train-<model>-<timestamp>.log (create logs/, gitignore it) alongside stdout; (2) the history dict train_model already builds (train_loss, epoch_times, metrics per epoch) dumped as trained/<model>_history.json next to the final checkpoint on every epoch (overwrite, crash-safe), so curves can be plotted and runs compared after the fact. No new dependencies — a tee-style writer and json.dump. Update tests/test_training.py.
----
Priority:   60
Status: ToDo
ID: api-zbgu

Implement HTTP API server for model inference and training management
----
Priority:   60
Status: Done
ID: seq-bounds-v5yr
Created: 2026-09-03T19:36:29Z
Updated: 2026-09-04T07:55:00Z

agi2/semantic: dataset-level boundary handling. TextDataset (src/dataset.py) slices training sequences blindly across utterance/document boundaries, so windows regularly straddle two unrelated utterances (the shakespeare_dialogue.txt corpus is ~31.6k utterances each terminated by <|endoftext|>). Once <|endoftext|> is a registered atomic special token, make the dataset boundary-aware: at minimum verify the marker survives as a single token in training sequences; better, align sequence starts to utterance boundaries so each window begins at the start of an utterance rather than mid-sentence (option: prevent windows from crossing a boundary, or accept crossing but always start on one). Consider making the behavior a TOML config flag. If variable-length sequences result, pad with the reserved <|pad|> token and mask padding out of the cosine-similarity loss. Requires the special-token registration work to land first; takes effect on the next retrain. Update tests/test_dataset.py.
----
Priority:   58
Status: ToDo
ID: mcp-cn3h

Implement MCP wrapper/server for model integration with MCP clients
----
Priority:   50
Status: ToDo
ID: ngram-tok-pei4

Implement an n-gram encoding tokenizer
----
Priority:   50
Status: Done
ID: freq-penalty-k9b2
Created: 2026-09-03T08:29:05Z
Updated: 2026-09-04T17:30:00Z

Add an inference-time frequency penalty to text generation in semantic/src/generation.py. Before sampling in generate_text, subtract penalty * count from each token's cosine-similarity score, where count is the number of times that token already appears in the generated output (not the prompt). Add a repetition_penalty TOML parameter (default 0.0 = off) read by the generation entry scripts and passed through generate_text; apply it after the corpus-vocab allowed_mask and before top-k/top-p. Cosine scores are bounded in [-1,1] before the temperature divide, so document a suggested range around 0.3-0.5 at temperature 0.3. Optionally also support no_repeat_ngram_size (default 0 = off) to hard-ban tokens completing an already-generated n-gram. Add unit tests (a penalized token's score drops proportionally to its count; disabled by default) and keep black/isort/mypy clean.
----
Priority:   50
Status: ToDo
ID: beam-stop-bmj4
Created: 2026-09-03T19:36:33Z
Updated: 2026-09-03T19:36:33Z

agi2/semantic: fix beam-search stop semantics in src/generation.py generate_with_beam_search. Current behavior: the loop breaks only when ALL beams have a stop token as their final token on the SAME step, and beams that hit a stop token keep being extended with more tokens on later steps — so a beam that finished cleanly gets junk appended unless every other beam finishes simultaneously, which almost never happens; in practice beam search runs to max_length and returns beams with text after their end markers. Standard fix: when a beam's last token is a stop token, freeze it (keep it as a completed candidate, do not extend it further; carry it forward in the beam list with its score, optionally length-normalized), keep expanding only unfinished beams, and stop when all beam_width beams are frozen or max_length is reached. Return completed beams truncated at their stop token. Update tests/test_generation.py with a case where one beam finishes before the others.
----
Priority:   46
Status: ToDo
ID: quant-ptq-zaqs

Implement post-training quantization (PTQ) for inference - convert FP32/FP16 to INT8/INT4 for 2-8x memory savings and faster computation on integer hardware. Add quantize_model method to model.py with torch.quantization.quantize_dynamic for Linear and LSTM layers.
----
Priority:   44
Status: ToDo
ID: quant-qat-gyx4

Implement quantization-aware training (QAT) - add quantization layers during training with QuantStub/DeQuantStub in transformer blocks. This enables better accuracy by training with quantization in mind rather than applying it post-training.
----
Priority:   42
Status: ToDo
ID: quant-amp-ywnh

Integrate mixed precision training with PyTorch AMP - use autocast and GradScaler for FP16 training to reduce memory usage and potentially speed up training while maintaining numerical stability.
----
Priority:   40
Status: ToDo
ID: ui-ugvz

Implement interactive prompted UI for text generation with real-time input/output